@page
@model Rain.Web.Areas.Identity.Pages.Account.ResendEmailConfirmationModel
@{
    ViewData["Title"] = "إعادة إرسال تأكيد البريد";
    Layout = "_AuthLayout";
}

@section Styles {
    <link rel="stylesheet" href="~/css/auth-styles.css" />
}

<div class="auth-container auth-theme-login">
    <div class="auth-card">
        <div class="auth-header position-relative">
            <div class="auth-icon mb-3">
                <i class="fas fa-paper-plane fa-2x"></i>
            </div>
            <h1 class="auth-title">أعد إرسال رابط التفعيل</h1>
            <p class="auth-subtitle">سنعيد إرسال رسالة التفعيل إلى بريدك الإلكتروني لتأكيد الحساب</p>
            <span class="company-badge">تأكيد الحساب</span>
        </div>

        <div class="auth-body">
            <ul class="auth-highlights">
                <li><i class="fas fa-envelope-open-text"></i>إرسال آمن وسريع إلى بريدك المسجل</li>
                <li><i class="fas fa-info-circle"></i>اطّلع على صندوق البريد أو الرسائل غير المرغوبة</li>
                <li><i class="fas fa-shield-alt"></i>الرابط صالح لوقت محدود لضمان الأمان</li>
            </ul>

            <form method="post" class="needs-validation" novalidate>
                <div class="mb-3">
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-at"></i></span>
                        <div class="form-floating flex-grow-1">
                            <input asp-for="Input.Email" class="form-control" placeholder="example@mail.com" required />
                            <label asp-for="Input.Email">البريد الإلكتروني المسجل</label>
                            <div class="invalid-feedback">يرجى إدخال بريد إلكتروني صحيح</div>
                        </div>
                    </div>
                    <span asp-validation-for="Input.Email" class="text-danger small"></span>
                </div>

                <div class="alert alert-info small" role="alert">
                    <i class="fas fa-lightbulb me-1"></i>
                    إذا لم تصلك الرسالة خلال دقائق، تحقق من مجلد الرسائل غير المرغوبة أو أعد المحاولة لاحقاً.
                </div>

                <button type="submit" class="btn btn-auth mb-3">
                    <i class="fas fa-sync-alt me-2"></i> إرسال رابط جديد
                </button>

                @if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
                {
                    <div class="alert alert-success small" role="alert">
                        <i class="fas fa-check-circle me-1"></i>@Model.StatusMessage
                    </div>
                }

                <div class="text-center mt-3 small">
                    <a asp-page="/Account/Login" class="text-info">العودة إلى تسجيل الدخول</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
